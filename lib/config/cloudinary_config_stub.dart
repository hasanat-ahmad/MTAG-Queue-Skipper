/// Default empty Cloudinary settings (used when [cloudinary_config.local.dart] is absent).
class CloudinaryLocalSecrets {
  CloudinaryLocalSecrets._();

  static const String cloudName = '';
  static const String uploadPreset = '';
}
